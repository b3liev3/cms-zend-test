# php-patterns-embed-zend-framework

This project contains **PHP Patterns**. They are inside the **libary** folder.

You can run the code as a webpage using the **Zend Structure**, but is not recommended.
